---
title: silent | 配置
outline: deep
---

# silent <CRoot /> {#silent}

- **类型:** `boolean | 'passed-only'`
- **默认值:** `false`
- **CLI:** `--silent`, `--silent=false`

静默测试的控制台输出。

使用 `'passed-only'` 仅查看失败测试的日志。失败测试的日志会在测试结束后打印。
